# API Configuration Example
# Copy this file to .env and update values as needed

# Environment Configuration
ENVIRONMENT=development  # Options: development, staging, production

# API URLs
# Leave these unset to use defaults or auto-detection
# API_BASE_URL=https://api.example.com  # Custom API URL (overrides all others)
# PRODUCTION_API_URL=https://api.quantumlayerplatform.com
# STAGING_API_URL=https://staging-api.quantumlayerplatform.com
# LOCAL_API_URL=http://localhost:8000

# Auto-detection
AUTO_DETECT_HOST=true  # Auto-detect host from request in development

# CORS/CSP Configuration
# Comma-separated list of additional allowed origins
# ADDITIONAL_ALLOWED_ORIGINS=https://app.example.com,https://dashboard.example.com

# CDN Configuration (optional)
# SWAGGER_CDN_URL=https://cdn.jsdelivr.net/npm/swagger-ui-dist@5.9.0
# REDOC_CDN_URL=https://cdn.jsdelivr.net/npm/redoc@2.0.0

# Examples for different environments:

# Production:
# ENVIRONMENT=production
# API_BASE_URL=https://api.quantumlayerplatform.com
# AUTO_DETECT_HOST=false

# Staging:
# ENVIRONMENT=staging
# API_BASE_URL=https://staging-api.quantumlayerplatform.com
# AUTO_DETECT_HOST=false

# Local Development (default):
# ENVIRONMENT=development
# AUTO_DETECT_HOST=true
# The API will auto-detect the current host/port

# Custom deployment:
# ENVIRONMENT=production
# API_BASE_URL=https://custom.yourdomain.com
# ADDITIONAL_ALLOWED_ORIGINS=https://app.yourdomain.com